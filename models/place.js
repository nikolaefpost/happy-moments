export class Place {
    constructor(title, imageUri, address, lat, lng, id) {
        this.title = title;
        this.imageUri = imageUri;
        this.address = address;
        this.lat = lat;
        this.lng = lng;
        this.id = id
    }
}